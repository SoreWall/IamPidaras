<Page x:Class="coursework01.Pages.CustomersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:coursework01.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CustomersPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="70"></RowDefinition>
        </Grid.RowDefinitions>
        
        <DataGrid Name="MainDG" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" IsReadOnly="True"></DataGridTextColumn>
                <DataGridTextColumn Header="Surname" Binding="{Binding Surname}" IsReadOnly="True"></DataGridTextColumn>
                <DataGridTextColumn Header="First name" Binding="{Binding FirstName}" IsReadOnly="True"></DataGridTextColumn>
                <DataGridTextColumn Header="Middle name" Binding="{Binding MiddleName}" IsReadOnly="True"></DataGridTextColumn>
                <DataGridTextColumn Header="Phone number" Binding="{Binding PhoneNumber}" IsReadOnly="True"></DataGridTextColumn>
                <DataGridTextColumn Header="Login" Binding="{Binding Login}" IsReadOnly="True"></DataGridTextColumn>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" IsReadOnly="True"></DataGridTextColumn>
                <DataGridTextColumn Header="Address" Binding="{Binding Address}" IsReadOnly="True"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1">
            <Button Name="BackBTN" Content="Вернуться" Style="{StaticResource RegisterButton}" Margin="0 10 0 0" Click="BackBTN_Click"/>
        </StackPanel>
    </Grid>
</Page>
